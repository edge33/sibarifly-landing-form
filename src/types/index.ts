export type LandingFormData = {
  date: Date;
  registration: string;
  model: string;
  pilotInCommand: string;
  firstOfficer?: string;
  paxNumber: number;
  departure: string;
  destination: string;
  arrivalTime: string;
  departureTime: string;
};
